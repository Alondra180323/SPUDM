Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^app/ - [F,L]
    RewriteRule ^storage/ - [F,L]
    RewriteRule ^\.env - [F,L]
</IfModule>

<FilesMatch "^(\.env|config\.php|permissions\.php)$">
    Require all denied
</FilesMatch>
